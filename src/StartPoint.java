/**
 * Created by Denis on 20.06.2015.
 */
public interface StartPoint {
    int y;   // перемення интерфейса
    int x;   // переменная интефрейса
    void getPoint(x, y) {
        this.x=x;
        this.y = y;
    }
    //public StartPoint(x, y);  сказ про то, как я пытаюсь создать конструктор интерфейса
}
